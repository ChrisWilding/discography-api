# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~
GET     /graphql                    controllers.GraphQLController.graphql(query: String, variables: Option[String], operation: Option[String])
POST    /graphql                    controllers.GraphQLController.graphqlBody
GET     /schema                     controllers.GraphQLController.schema
GET     /hello/:name                controllers.HelloController.hello(name: String)
GET     /playground                 controllers.PlaygroundController.playground
